# Mini-CNN

#command to add all changes
git add .

#command to comit changes
git commit -m "initial config done"

```
git push origin main
```

bash ./init_script.sh

touch app.py
mkdir src
mkdir models
touch Developments.md
touch src/train_model.py
touch src/test_model.py
